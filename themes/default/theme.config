# Theme Configuration: default
# Defines which image resolutions this theme requires

# Image resolutions (space-separated widths in pixels)
# The theme's responsive design uses these breakpoints
resolution=(3840 2560 1920 1280 1024 640 320 160 80 20)

# Optional: JPEG quality can be overridden per theme
# jpeg_quality=90
